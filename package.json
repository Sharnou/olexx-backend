{
  "name": "olexx-backend",
  "version": "0.1.0",
  "description": "Lightweight classifieds backend with in-memory search, optional Elasticsearch/RabbitMQ, and AWS S3 presign support.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "node test.js",
    "lint": "echo \"No linter configured\" && exit 0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.554.0",
    "@aws-sdk/s3-request-presigner": "^3.554.0",
    "amqplib": "^0.10.3",
    "better-sqlite3": "^9.6.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
